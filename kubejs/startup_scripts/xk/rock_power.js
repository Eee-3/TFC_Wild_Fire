/*ItemEvents.modelProperties(event => {
    event.register('kubejs:rock_powder', new ResourceLocation('kubejs:has_fluid'), (item) => {
        let tank = item.getCompound("tank");
let amount = tank.getInt("Amount");
        if (item.hasNBT()) {if(amount>50){return 1;}    
                
            }
        
        return 0; 
    });
});*/